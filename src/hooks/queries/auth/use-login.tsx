import React from "react";

type UseLoginProps = {
  email: string;
  password: string;
};

const UseLogin = ({ email, password }: UseLoginProps) => {
  return <div></div>;
};

export default UseLogin;
